services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: agency2-federation:latest
    container_name: agency2-federation
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      PORT: 3000
      HOSTNAME: 0.0.0.0
